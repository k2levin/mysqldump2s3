version:          "3"
services:
  db:
    image:        mysql:latest
    environment:
      - MYSQL_ROOT_PASSWORD=supersecret
  app:
    image:        k2levin/php-laravel:misc
    deploy:
      resources:
        limits:
          cpus: '0.8'
          memory: 5G
    environment:
      - TZ=Asia/Singapore
    volumes:
      - "$PWD/:/var/www/app/mysqldump2s3/"
